# A patch belongs here the moment a shape changes under a site that already
# exists. Everything before the first entry below was created from this tree
# and needed nothing.

[pre_model_sync]

[post_model_sync]
# A drop folder's credentials stop being OneMobility's and become a mount the
# Drive can browse. Post-sync, because it reads a column the sync adds.
oneapp.patches.sftp_sources_become_mounts
oneapp.patches.sources_become_folders
oneapp.patches.the_assistant_keeps_its_own_name
# The work moves onto ERPNext's own Task and Project before `One Task` and
# `One Project` are dropped — `docs/WORK.md` §12. Post-sync, because it writes
# the custom fields the sync adds.
oneapp.patches.the_work_becomes_erpnexts
# The response target gains its priorities, its second clock and a rule list —
# `docs/ONECRM.md` stage 6. Moves `hours` into a default level row and the
# `when_field`/`when_value` pair into a rule, before the schema sync drops the
# three columns.
oneapp.patches.a_target_gains_its_priorities
