"""OneCRM — ERPNext's CRM module, with the things its schema cannot say.

`docs/ONECRM.md` is the argument. Their `Lead` is the lead, their `Opportunity`
is the deal, their `Quotation` is the quote and their `Customer` is the other
side of a won one. Nothing here is a second CRM: the rule is OneHR's, and
what lives in this directory is the presentation's other half — the stage a
pipeline is drawn by, and the behaviour that keeps ERPNext's own fields written
from it.
"""
