# AI

OneAI does nothing here yet.

The obvious thing it could do is write the form: name a doctype and a purpose,
and get a field list and an introduction. That is worth having and is not stage
1 — a builder has to exist before something can fill it in — and it is the kind
of feature that is only useful if what it produces is exactly what a person
would have dragged into place themselves.

Nothing is configured per tenant.
